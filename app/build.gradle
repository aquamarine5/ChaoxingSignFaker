/*
 * Copyright (c) 2025-2026, @aquamarine5 (@海蓝色的咕咕鸽). All Rights Reserved.
 * Author: aquamarine5@163.com (Github: https://github.com/aquamarine5) and Brainspark (previously RenegadeCreation)
 * Repository: https://github.com/aquamarine5/ChaoxingSignFaker
 */

import org.aquamarine5.brainspark.stackbricks.gradleplugin.PossibleConfigurationPath

plugins {
    alias(libs.plugins.android.application)
    alias(libs.plugins.kotlin.compose)
    id("org.aquamarine5.brainspark.stackbricks-gradle-plugin") version "2.2.9"
    id("com.google.protobuf") version "0.9.6"
    id "io.sentry.android.gradle" version "6.19.0"
    id("org.jetbrains.kotlin.plugin.serialization") version "2.4.10"
}

sentry {
    org = "aquamarine5"
    projectName = "chaoxingsignfaker"
    authToken = System.getenv("SENTRY_AUTH_TOKEN") ?: findProperty("sentry.authToken")
}

stackbricksConfig {
    possibleConfigurationPaths = [
            new PossibleConfigurationPath("cdn.aquamarine5.fun", "chaoxingsignfaker_stackbricks_manifest.json"),
            new PossibleConfigurationPath("cdn.aquamarine5.fun", "chaoxingsignfaker_stackbricks_v2_manifest.json")
    ]
    changelog =
            "暑假的时候作者还在积极更新！带来一个更好的随地大小签\n以下是暑假期间的新版本更新内容：" +
                    "# 1.17.0 更新：\n1. 支持存储自己和代签用户的人脸识别照片，就不用每次自己找了。\n2. 现在自己的代签二维码会附带自己的人脸识别照片，重新扫描其他人的新版本二维码时会把你的照片下载到他的设备上存储。\n\n"+
                    "# 1.16.* 更新：\n1. 由于学习通更新导致原接口失效，修复了获取群聊列表失败的问题。\n\n" +
                    "# 1.15.* 更新：\n1. 支持克隆登录代签用户的账号以给他有你没有的课程签到\n\n" +
                    "lovely but not lonely, 20260731 life need dimoo, @aquamarine5 Brainspark."
    requiredManifestVersion2MigrateForceInstall = true
    forceInstallLessVersion = 1_1407_2000
    qiniuConfig {
        accessKey = project.findProperty("qiniu.accessKey") ?: ""
        secretKey = project.findProperty("qiniu.secretKey") ?: ""
        bucket = "aquabucket"
        referer = project.findProperty("qiniu.referer") ?: ""
        println("Qiniu config: $accessKey, $secretKey, $bucket, $referer")
    }
}
android {
    namespace = "org.aquamarine5.brainspark.chaoxingsignfaker"
    compileSdk = 37

    defaultConfig {
        ndk {
            //noinspection ChromeOsAbiSupport
            abiFilters "arm64-v8a"
        }
        applicationId = "org.aquamarine5.brainspark.chaoxingsignfaker"
        minSdk = 26
        targetSdk = 37
        versionCode = 1_1700_1009
        versionName = "1.17.0-rc1-260824"

        testInstrumentationRunner = "androidx.test.runner.AndroidJUnitRunner"
    }

    signingConfigs {
        register("chaoxingsignfaker") {
            storeFile file("..\\chaoxingsignfaker.jks")
            storePassword System.getenv("keystorePassword")
            keyAlias "chaoxingsignfaker"
            keyPassword System.getenv("keystorePassword")
        }
    }
    buildFeatures {
        buildConfig = true
    }
    buildTypes {
        release {
            optimization {
                enable = true // Enables code and resource optimizations.
            }
            minifyEnabled true
            shrinkResources true
            proguardFiles(
                    getDefaultProguardFile("proguard-android-optimize.txt"),
                    "proguard-rules.pro"
            )
            signingConfig signingConfigs.chaoxingsignfaker
            buildConfigField("String", "releaseDate", "\"${new Date().format('yyyy-MM-dd HH:mm:ss')}\"")
            buildConfigField("String", "umengChannel", "\"${System.getenv("UMENG_CHANNEL")}\"" ?: '"Stackbricks"')
            buildConfigField("String", "imEncryptedKey", "\"${project.findProperty("imEncryptedKey") ?: System.getenv("imEncryptedKey") ?: ""}\"")
        }
        debug {
            signingConfig signingConfigs.chaoxingsignfaker
            buildConfigField("String", "releaseDate", "\"${new Date().format('yyyy-MM-dd HH:mm:ss')}\"")
            buildConfigField("String", "umengChannel", "\"${System.getenv("UMENG_CHANNEL")}\"" ?: '"Stackbricks"')
            buildConfigField("String", "imEncryptedKey", "\"${project.findProperty("imEncryptedKey") ?: System.getenv("imEncryptedKey") ?: ""}\"")
        }
    }

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_21
        targetCompatibility = JavaVersion.VERSION_21
    }
    buildFeatures {
        compose = true
    }
}

composeCompiler {
    reportsDestination = layout.buildDirectory.dir("compose_compiler")
    metricsDestination = layout.buildDirectory.dir("compose_compiler")
}

protobuf {
    protoc {
        artifact = "com.google.protobuf:protoc:4.35.1"
    }

    generateProtoTasks {
        all().each { task ->
            task.builtins {
                java {
                    option 'lite'
                }
            }
        }
    }
}

dependencies {
//    implementation 'io.hyphenate:hyphenate-chat:4.9.0.1'
    implementation(libs.reorderable)
    implementation(libs.lbsyun.base)
    implementation libs.patternlocker
    implementation libs.androidx.material
    implementation(libs.colorpicker.compose)
    implementation libs.ui
    implementation libs.core
    implementation libs.camera.mlkit.vision
    implementation libs.barcode.scanning
    implementation libs.androidx.camera.core
    implementation libs.androidx.camera.camera2
    implementation libs.androidx.camera.lifecycle
    implementation libs.androidx.camera.view
    implementation(libs.coil.compose)
    implementation(libs.coil.network.okhttp)
    implementation libs.accompanist.permissions
    implementation libs.baidumapsdk.location
    implementation libs.stackbricks
    implementation libs.common
    implementation libs.asms
    implementation libs.kotlinx.serialization.json
    implementation libs.androidx.navigation.compose
    implementation libs.androidx.datastore
    implementation libs.baidumapsdk.map
    implementation libs.baidumapsdk.search
    implementation libs.protobuf.javalite
    implementation(libs.fastjson2)
    implementation(libs.okhttp)
    implementation(libs.androidx.core.ktx)
    implementation(libs.androidx.lifecycle.runtime.ktx)
    implementation(libs.androidx.activity.compose)
    implementation(libs.androidx.activity.ktx)
    implementation(platform(libs.androidx.compose.bom))
    implementation(libs.androidx.ui)
    implementation(libs.androidx.ui.graphics)
    implementation(libs.androidx.ui.tooling.preview)
    implementation(libs.androidx.material3)
    testImplementation(libs.junit)
    androidTestImplementation(libs.androidx.junit)
    androidTestImplementation(libs.androidx.espresso.core)
    androidTestImplementation(platform(libs.androidx.compose.bom))
    androidTestImplementation(libs.androidx.ui.test.junit4)
    debugImplementation(libs.androidx.ui.tooling)
    debugImplementation(libs.androidx.ui.test.manifest)
}